Into to reset lab.
reset soft example text.
reset hard warning example text.

## Reset Observations

**Soft reset :- It undoes the commit but keeps the changes staged to recommit
**Mixed reset :- It undoes both staging and commit but keeps the changes made in the file as it is.
**Hard reset :- It completely resets the uncommitted changes from the files and resets to the previous commit.