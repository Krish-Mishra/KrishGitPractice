# PR Merge Strategies Summary

1. Merge commit :- It merges all the commits and one extra merge commit.
2. Squash and Merge :- It creates one unified commit for all the commits which keeps the merge history clean.
3. Rebase and Merge :- It makes seperate commits but do not make extra merge commit and also manages the history of base branch.